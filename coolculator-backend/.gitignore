# Build output
/bin/

# Generated Swagger/OpenAPI spec — regenerated by `make swagger`, never
# hand-edited. Kept out of version control so the handler annotations stay
# the single source of truth; `make run`/`build`/`test` regenerate it first.
/docs/

# Test artifacts
coverage.out
*.test
