# Optional — docker-compose.yml works with none of these set (falls back
# to dev/unknown). Copy to .env to customize the backend's build metadata,
# e.g. from CI: VERSION=$(git describe --tags), COMMIT=$(git rev-parse --short HEAD).
VERSION=
COMMIT=
BUILD_TIME=
